import React, { FC } from 'react';

const Auth: FC = (props) => <div className="AuthWrapper">Test content</div>;

Auth.propTypes = {
  // bla: PropTypes.string,
};

Auth.defaultProps = {
  // bla: 'test',
};

export default Auth;
